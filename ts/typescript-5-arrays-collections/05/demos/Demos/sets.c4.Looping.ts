// set up the set variable with instantiated data
let gameSet = new Set<string>(['Tetris', 'Pong', 'The Oregon Trail']);


// grab all the items from the set
for (let game of gameSet) {
    console.log(game);
}